import math
import helpers.input_reader as ir

FILE_PATH = 'input/day8test.txt'

def print_lines(lines: list[str]):
    for line in lines:
        print(line)

def part_one():
    inputs = ir.read_file(FILE_PATH)
    # print_lines(inputs)
    return None